export { API_CONFIG, ENV_CONFIG } from './api';
export { makeStyles, useTheme } from './Colors';

export const APP_CONFIG = {
  name: 'Green Social',
  version: '1.0.0',
  description: 'A social app for environmental awareness',
} as const;

